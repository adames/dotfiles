# CLAUDE.md

Global user memory. Hashimoto style
([mitchellh/nixos-config](https://github.com/mitchellh/nixos-config)) —
narrative, opinionated. *"This isn't turnkey. I value X over Y. Don't
do A — we tried it."*

Every line earns its place. Pointer, don't duplicate. Repo beats
project memory beats this file.
