# CLAUDE.md

Global user memory. Default tonal anchor for any agent-instruction
file in this account: **Hashimoto style**
([`mitchellh/nixos-config`](https://github.com/mitchellh/nixos-config))
— narrative, opinionated, humble caveats. *"This isn't turnkey. I
value X over Y. Don't do A — we tried it."*

Every line earns its place. If removing it wouldn't cause a specific
mistake, cut it. Pointer, don't duplicate. ~100 line ceiling per
agent file; if you're past it, you haven't decided what matters.

Repo beats project memory beats this file. Don't restate what
something more specific already said.
