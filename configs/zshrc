export PATH="$HOME/.local/bin:$HOME/bin:/usr/local/bin:$PATH"
typeset -U PATH

# vi-mode FIRST: ZLE consumers (fzf, autosuggestions) bind to the active
# keymap at source time. Switching after them loses Ctrl-R / Ctrl-T.
bindkey -v

eval "$(starship init zsh)"

# brew on macOS, ~/.zsh git-clone on Linux
for _zp in \
  /opt/homebrew/share/zsh-autosuggestions/zsh-autosuggestions.zsh \
  ~/.zsh/zsh-autosuggestions/zsh-autosuggestions.zsh
do [[ -f "$_zp" ]] && { source "$_zp"; break; }; done
for _zp in \
  /opt/homebrew/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh \
  ~/.zsh/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
do [[ -f "$_zp" ]] && { source "$_zp"; break; }; done
unset _zp

# fzf, fd-backed (respects .gitignore by default)
if command -v fd &>/dev/null; then
  export FZF_DEFAULT_COMMAND='fd --type f --hidden --follow --exclude .git'
  export FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"
  export FZF_ALT_C_COMMAND='fd --type d --hidden --follow --exclude .git'
fi
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

HISTSIZE=10000
SAVEHIST=10000
HISTFILE=~/.zsh_history
setopt HIST_IGNORE_DUPS HIST_IGNORE_SPACE SHARE_HISTORY

export EDITOR='vim'
export RIPGREP_CONFIG_PATH="$HOME/.ripgreprc"

alias ll='ls -lah'
alias la='ls -A'
alias ..='cd ..'
alias ...='cd ../..'
alias gs='git status'
alias ga='git add'
alias gc='git commit'
alias gp='git push'
alias gpl='git pull'

# mise: brew on mac, ~/.local/bin on linux
if   command -v mise        &>/dev/null; then eval "$(mise activate zsh)"
elif [ -f ~/.local/bin/mise ];                then eval "$(~/.local/bin/mise activate zsh)"
fi

command -v direnv &>/dev/null && eval "$(direnv hook zsh)"
command -v zoxide &>/dev/null && eval "$(zoxide init zsh)"

# Workspace metadata. ~/.cache/workspace/current.env is rewritten on every
# macOS space switch by the yabai signal handler; sourcing on precmd keeps
# the shell + starship's custom.workspace module fresh even in long-lived
# panes that pre-dated the latest rename.
_workspace_load() {
  [[ -r "$HOME/.cache/workspace/current.env" ]] && source "$HOME/.cache/workspace/current.env"
}
autoload -U add-zsh-hook
add-zsh-hook precmd _workspace_load
_workspace_load

# Auto-attach tmux on SSH
if [[ -n "$SSH_CONNECTION" && -z "$TMUX" ]]; then
  tmux attach-session -t main 2>/dev/null || tmux new-session -s main
fi
