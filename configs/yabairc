#!/usr/bin/env sh
# yabai BSP tiling. Hotkeys in ~/.skhdrc.

yabai -m config layout                      bsp
yabai -m config window_placement            second_child
yabai -m config window_gap                  8
yabai -m config top_padding                 8
yabai -m config bottom_padding              8
yabai -m config left_padding                8
yabai -m config right_padding               8
yabai -m config split_ratio                 0.50
yabai -m config auto_balance                off

yabai -m config mouse_follows_focus         off
yabai -m config focus_follows_mouse         off
yabai -m config mouse_modifier              fn
yabai -m config mouse_action1               move
yabai -m config mouse_action2               resize
yabai -m config mouse_drop_action           swap

# Float system dialogs / utility apps
yabai -m rule --add app="^System Settings$"     manage=off
yabai -m rule --add app="^System Preferences$"  manage=off
yabai -m rule --add app="^Calculator$"          manage=off
yabai -m rule --add app="^Karabiner-Elements$"  manage=off
yabai -m rule --add app="^Hammerspoon$"         manage=off
yabai -m rule --add app="^Finder$" title="^(Copy|Move|Info)" manage=off

# Ghostty: don't manage. AppKit native tabs each create a long-lived NSWindow
# that yabai gives its own BSP slot, halving the visible window on every Cmd+T.
# yabai has no concept of AppKit tab groups, so the float-on-create dance
# doesn't undo the split. Keep one big Ghostty window, use tmux for splits.
yabai -m signal --remove ghostty_tab_dance 2>/dev/null   # remove old approach if present
yabai -m rule --add app="^[Gg]hostty$" manage=off

echo "yabai configuration loaded"
