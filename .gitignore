# macOS
.DS_Store
._*

# Backups created by bootstrap.sh
*.bak

# Editor
.vscode/
.idea/
*.swp

# ============================================================================
# AI AGENT WORKSPACES — Per-agent worktrees + live settings live under
# ~/personas/.<agent>/, never inside this repo. Agent instruction files
# (AGENTS.md, CLAUDE.md, etc.) ship in-tree; keep them minimal.
# ============================================================================

# Agent worktrees (if accidentally created here)
.agent/
.claude/
.windsurf/
.windsurf-cascade/
.devin/
.cursor/
.aider/
.github-copilot/
.codewhisperer/

# Agent settings (per-agent or shared live copies)
.settings.local.json
settings.local.json
settings.*.json
*.agent.json
.agent.env
configs/workspace/*
!configs/workspace/cheatsheet-layout.json
